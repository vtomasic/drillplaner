import React from 'react'
import ReactDOM from 'react-dom/client'
import Uebungsplaner from './uebungsplaner'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Uebungsplaner />
  </React.StrictMode>,
)
